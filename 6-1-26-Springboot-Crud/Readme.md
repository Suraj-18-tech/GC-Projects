# Student CRUD Application (Spring Boot + MongoDB)

This is a simple **CRUD (Create, Read, Update, Delete)** application built using **Spring Boot** and **MongoDB**.  
It exposes REST APIs to manage student data and can be tested using **Postman**.

---

## 🚀 Technologies Used
- Java 21  
- Spring Boot  
- Maven  
- MongoDB  
- Postman (for API testing)

---

## 🔐 Environment Variables

Create a `.env` file in the project root and add:
MONGODB_URI=your_mongodb_connection_string

---

## ▶️ How to Run the Project
.\mvnw spring-boot:run
